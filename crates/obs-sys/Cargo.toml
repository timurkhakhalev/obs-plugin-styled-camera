[package]
name = "obs-sys"
version = "0.1.0"
edition = "2021"
build = "build.rs"
links = "obs"
publish = false
license = "GPL-2.0-or-later"
description = "Unsafe OBS Studio libobs bindings (bindgen)"

[lib]
path = "src/lib.rs"

[dependencies]
libc = "0.2"

[build-dependencies]
bindgen = "0.71"
