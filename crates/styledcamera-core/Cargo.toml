[package]
name = "styledcamera-core"
version.workspace = true
edition.workspace = true
publish = false
license.workspace = true
description = "Pure logic for StyledCamera (OBS plugin): mask postprocess and helpers"

[lib]
path = "src/lib.rs"
